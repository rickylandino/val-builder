sonar.projectKey=rickylandino_val-builder
sonar.organization=automated-concepts

# This is the name and version displayed in the SonarCloud UI.
#sonar.projectName=val-builder
#sonar.projectVersion=1.0

# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
sonar.sources=src
sonar.tests=src/test
sonar.test.inclusions=src/test/**/*.test.tsx,src/test/**/*.test.ts

# Coverage reports
sonar.javascript.lcov.reportPaths=coverage/lcov.info
sonar.coverage.exclusions=**/*.test.tsx,**/*.test.ts,**/test/**,**/*.config.ts,**/*.config.js,**/config/**,

# Encoding of the source code. Default is default system encoding
sonar.sourceEncoding=UTF-8